apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namePrefix: dozzle-
namespace: default

resources:
  - service-account.yaml
  - cluster-role.yaml
  - cluster-role-binding.yaml
  - pv.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
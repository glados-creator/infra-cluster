apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-pvc
  namespace: default
  labels:
    app: main-dozzle
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs
  volumeName: dozzle-pv
  resources:
    requests:
      storage: 1Gi

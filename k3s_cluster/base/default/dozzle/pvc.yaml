apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-pvc
  namespace: default
  labels:
    app: main-dozzle
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: "" # cephfs
  volumeName: pv
  resources:
    requests:
      storage: 1Gi
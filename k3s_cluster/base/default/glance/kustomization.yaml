apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namePrefix: glance-
namespace: default

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

configMapGenerator:
  - name: glance-config
    files:
      - glance.yml=config.yaml
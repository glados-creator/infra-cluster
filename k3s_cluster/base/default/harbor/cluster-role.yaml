apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: clusterrole
  namespace: default 
  labels:
    app: main-harbor
rules:
  - apiGroups: [""]
    resources:
    - pods
    - namespaces
    - "*"
    verbs: ["get", "list", "watch"]
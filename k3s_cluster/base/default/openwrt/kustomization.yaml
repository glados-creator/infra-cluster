apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namePrefix: openwrt-
namespace: default

resources:
  - multus-config.yaml
  - pv.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
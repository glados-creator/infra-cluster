apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-pvc
  namespace: default
  labels:
    app: main-openwrt
spec:
  accessModes: [ReadWriteMany]
  storageClassName: nfs
  volumeName: pv
  resources:
    requests:
      storage: 5Gi
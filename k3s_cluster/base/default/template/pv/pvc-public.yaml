apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-pvc
  namespace: public
  labels:
    app: public-<app-name>
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: "" # cephfs
  volumeName: pv
  resources:
    requests:
      storage: <app-cap>Gi

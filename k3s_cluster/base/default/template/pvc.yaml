---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-pvc
  namespace: <app-ns>
  labels:
    app: <app-ns>-<app-name>
spec:
  accessModes: [ReadWriteMany]
  storageClassName: nfs
  volumeName: <app-name>-pv
  resources:
    requests:
      storage: 256Mi
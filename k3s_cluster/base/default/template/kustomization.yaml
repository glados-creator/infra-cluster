apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namePrefix: <app-name>-
namespace: default

resources:
  - service-account.yaml
  - cluster-role.yaml
  - cluster-role-binding.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-pvc
  namespace: default
  labels:
    app: main-netboot
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs
  volumeName: pv
  resources:
    requests:
      storage: 10Gi

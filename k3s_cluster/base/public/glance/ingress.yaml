apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ingressroute
  namespace: public
  labels:
    app: public-glance
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - kind: Rule
      match: Host(`glance.public.home`)
      middlewares:
       - name: public-middleware-auth-authentik
      services:
        - name: public-glance-service
          port: 9900
  #tls: {} 
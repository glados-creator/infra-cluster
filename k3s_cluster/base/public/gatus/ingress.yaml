apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ingressroute
  namespace: public
  labels:
    app: public-gatus
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - kind: Rule
      match: Host(`gatus.public.home`)
      middlewares:
       - name: public-middleware-basic-auth
      services:
        - name: gatus-service-dashboard
          port: 9901
  #tls: {} 
apiVersion: v1
kind: Service
metadata:
  name: service
  namespace: public
  labels:
    app: public-gatus
spec:
  type: ClusterIP
  selector:
    app: public-gatus
  ports:
    - name: web
      protocol: TCP
      port: 9901
      targetPort: 8080

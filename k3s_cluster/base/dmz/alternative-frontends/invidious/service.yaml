apiVersion: v1
kind: Service
metadata:
  name: service
  namespace: dmz
  labels:
    app: dmz-frontalt-invidious
spec:
  type: ClusterIP
  selector:
    app: dmz-frontalt-invidious
  ports:
    - name: web
      protocol: TCP
      port: 6615
      targetPort: 3000

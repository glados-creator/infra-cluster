apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-pvc
  namespace: dmz
  labels:
    app: dmz-frontalt-searxng
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: "" # cephfs
  volumeName: pv
  resources:
    requests:
      storage: 10Gi

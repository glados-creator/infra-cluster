apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namePrefix: gatus-
namespace: dmz

resources:
  - service-account.yaml
  - pvc.yaml
  - pv.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

configMapGenerator:
  - name: gatus-config
    files:
      - config.yaml
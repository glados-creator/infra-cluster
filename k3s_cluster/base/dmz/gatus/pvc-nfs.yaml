apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-pvc
  namespace: dmz
  labels:
    app: dmz-gatus
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs
  volumeName: pv
  resources:
    requests:
      storage: 1Gi

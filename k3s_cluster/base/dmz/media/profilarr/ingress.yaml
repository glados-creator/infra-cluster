apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ingressroute
  namespace: dmz
  labels:
    app: dmz-media-profilarr
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - kind: Rule
      match: Host(`profilarr.media.dmz.home`)
      middlewares:
        - name: dmz-media-middleware-auth-trauth # authentik
      services:
        - name: dmz-media-profilarr-service
          port: 6668
  #tls: {} 
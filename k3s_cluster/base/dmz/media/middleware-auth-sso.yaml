apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: middleware-auth-sso
  namespace: dmz
spec:
  plugin:
    trauth:
      domain: "media.dmz.home"
      # cookiename: "trauth_cookie"
      # cookiepath: "/"
      # cookiesecure: false   # set true if using HTTPS
      # cookiehttponly: true
      usersfile: "/etc/trauth/auth"
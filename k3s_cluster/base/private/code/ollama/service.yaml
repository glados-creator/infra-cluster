apiVersion: v1
kind: Service
metadata:
  name: service
  namespace: private
  labels:
    app: private-ollama
spec:
  type: ClusterIP
  selector:
    app: private-ollama
  ports:
    - name: web
      protocol: TCP
      port: 4431
      targetPort: 11434
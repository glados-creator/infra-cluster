apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-pvc
  namespace: private
  labels:
    app: private-code-webui
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: "" # cephfs
  volumeName: pv
  resources:
    requests:
      storage: 10Gi

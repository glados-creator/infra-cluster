apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namePrefix: webui-
namespace: private
resources:
  - sa.yaml
  - pvc-cephfs.yaml
  - pv-cephfs.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
configMapGenerator:
  - name: webui-config
    envs:
      - config.env
generatorOptions:
  disableNameSuffixHash: false

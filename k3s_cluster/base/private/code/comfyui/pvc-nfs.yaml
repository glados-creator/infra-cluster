apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-pvc
  namespace: private
  labels:
    app: private-code-comfyui
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs
  volumeName: pv
  resources:
    requests:
      storage: 100Gi

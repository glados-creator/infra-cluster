apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namePrefix: openclaw-
namespace: private

resources:
  - pvc.yaml
  - pv.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

configMapGenerator:
- name: openclaw-config
  envs:
  - config.env

generatorOptions:
  disableNameSuffixHash: false
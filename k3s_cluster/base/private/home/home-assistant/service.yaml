apiVersion: v1
kind: Service
metadata:
  name: service
  namespace: private
  labels:
    app: private-home-assistant
spec:
  type: ClusterIP
  selector:
    app: private-home-assistant
  ports:
    - name: web
      protocol: TCP
      port: 4404
      targetPort: 8123